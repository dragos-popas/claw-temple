# CLAW-TEMPLE Environment Variables
# Copy this to .env and fill in the values

# OpenClaw Configuration
OPENCLAW_DIR=/home/dp420/.openclaw
OPENCLAW_GATEWAY_URL=http://localhost:11434

# Server Configuration
PORT=3000
HOST=0.0.0.0

# SQLite Database Path
DATABASE_PATH=./data/claw-temple.db

# Worktree Base Directory
WORKTREE_BASE=./worktrees

# OpenRouter (uses OpenClaw's config by default)
# OPENROUTER_API_KEY=sk-...

# GitHub (uses OpenClaw's git config by default)

# Notifications
ENABLE_BROWSER_NOTIFICATIONS=true
ENABLE_TELEGRAM_NOTIFICATIONS=true